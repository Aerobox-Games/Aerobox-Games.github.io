import React from 'react'
import ReactDOM from 'react-dom/client'
import RogueRacer from '../pages/RougeRacer.tsx';

import '../index.css'

ReactDOM.createRoot(document.getElementById('root')!).render(
  <React.StrictMode>
    <RogueRacer />
  </React.StrictMode>
)
